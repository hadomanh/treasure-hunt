package ngakinz.enums;

public enum MessageHeader {
	
	// Server
	START, ACCEPT, DENY, WAITING, RECEIVING, RESULT, 
	
	REMAIN_TREASURE, 
	
	NICE_PLAYER, MEAN_PLAYER, 
	
	UPDATE,
	
	TREASURE, CLUE,
	
	FINISH, 
	
	// Client
	MOVE, REPLAY, EXIT, USERNAME

}
